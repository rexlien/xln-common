xln:
  etcd-config:
    endPoint:
      name: etcd
    configNamespace: "local"
    enableVersionMeter: false
  cluster-config:
    port: 0
  dtask-config:
    root: xln-dtask-startup-test
    claimTtl: 10
    resyncInterval: 300000

xln.service-config:
  resourcePath: []
